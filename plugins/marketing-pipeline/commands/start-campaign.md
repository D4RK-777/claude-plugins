---
description: Start a new campaign in the Marketing Command Center
---

Open the Marketing Command Center dashboard and start a new campaign.

If the dashboard isn't installed yet, invoke the `setup-marketing-command-center` skill to install it first.

If it IS installed, just remind the user to open the existing dashboard and click "+ New campaign".
